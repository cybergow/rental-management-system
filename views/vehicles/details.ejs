<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Vehicle Details</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #f0f2f5;
      margin: 0;
      padding: 20px;
      display: flex;
      justify-content: center;
    }

    .vehicle-card {
      background: #fff;
      border-radius: 15px;
      box-shadow: 0 10px 20px rgba(0,0,0,0.15);
      padding: 30px;
      max-width: 700px;
      width: 100%;
      transform-style: preserve-3d;
      animation: fadeInUp 1s ease forwards;
    }

    .vehicle-card h2 {
      margin-top: 0;
      font-size: 28px;
    }

    .vehicle-image {
      width: 100%;
      max-height: 300px;
      object-fit: cover;
      border-radius: 12px;
      transition: transform 0.4s ease;
    }

    .vehicle-image:hover {
      transform: scale(1.05);
    }

    .details {
      margin: 15px 0;
      font-size: 16px;
    }

    .details strong {
      color: #333;
    }

    .status {
      padding: 6px 12px;
      background-color: #d4edda;
      color: #155724;
      display: inline-block;
      border-radius: 5px;
      margin: 10px 0;
    }

    .form-booking {
      margin-top: 20px;
      padding: 20px;
      border-top: 1px solid #ccc;
    }

    .form-booking label {
      display: block;
      margin-bottom: 10px;
    }

    .form-booking input[type="date"] {
      padding: 8px;
      margin-top: 5px;
      border: 1px solid #ccc;
      border-radius: 8px;
      width: 100%;
    }

    .btn-book {
      margin-top: 15px;
      padding: 12px 20px;
      font-size: 16px;
      background-color: #28a745;
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .btn-book:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 16px rgba(0,0,0,0.2);
    }

    .back-link {
      display: block;
      margin-top: 30px;
      color: #007bff;
      text-decoration: none;
      font-weight: bold;
    }

    .back-link:hover {
      text-decoration: underline;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(50px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>
  <div class="vehicle-card">
    <h2><%= vehicle.name %></h2>
    <img src="<%= vehicle.image_url %>" alt="<%= vehicle.name %>" class="vehicle-image" />

    <div class="details">
      <p><strong>Brand:</strong> <%= vehicle.brand %></p>
      <p><strong>Model:</strong> <%= vehicle.model %></p>
      <p><strong>Rent Per Day:</strong> ₹<%= vehicle.rent_per_day %></p>
      <p class="status"><%= vehicle.available ? 'Available' : 'Not Available' %></p>
    </div>

    <div class="form-booking">
      <h3>Book This Vehicle</h3>
      <form method="POST" action="/bookings/<%= vehicle.id %>">
        <label>
          Start Date:
          <input type="date" name="startDate" required />
        </label>
        <label>
          End Date:
          <input type="date" name="endDate" required />
        </label>
        <button type="submit" class="btn-book">Book Now</button>
      </form>
    </div>

    <a href="/vehicles" class="back-link">← Back to Vehicle List</a>
  </div>
</body>
</html>
