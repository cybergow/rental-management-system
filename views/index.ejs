<!DOCTYPE html>
<html>
<head>
  <title>Home</title>
  <link rel="stylesheet" href="/css/home.css">
  <style>
    /* Full Screen Video Background */
    .background-video {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover; /* Ensure the video covers the entire screen */
      z-index: -1; /* Keep the video behind the content */
    }

    /* Centered Text Styling for Heading */
    h1 {
      position: absolute;
      top: 40%; /* Adjust vertical position to place the heading closer to the center */
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 50px;
      color: white;
      text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.7);
      margin: 0; /* Remove any margin */
      text-align: center; /* Ensure the text is centered */
    }

    /* Centered Logged-in Section */
    .logged-in {
      position: absolute;
      top: 55%;  /* Adjusted vertical position for login/register links to be a bit higher */
      left: 50%;
      transform: translateX(-50%); /* Centers it horizontally */
      color: white;
      font-size: 20px;
      text-align: center;
      text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.7);
    }

    /* Links for Login/Register */
    .logged-in a {
      color: #fff;
      text-decoration: none;
      margin: 10px;
      font-size: 18px;
      display: inline-block; /* Ensures that links are aligned next to each other */
    }

    .logged-in a:hover {
      text-decoration: underline;
    }

  </style>
</head>
<body>
  <!-- Video Background -->
  <video autoplay muted loop class="background-video">
    <source src="/videos/video5.mp4" type="video/mp4">
    Your browser does not support the video tag.
  </video>
  
  <!-- Heading and Content -->
  <h1>Welcome to Bike & Car Rental</h1>
  <% if (session && session.user) { %>
    <div class="logged-in">
      <p>Hello, <%= session.user.name %>!</p>
      <a href="/vehicles">View Vehicles</a> | <a href="/bookings/history">My Bookings</a>
    </div>
  <% } else { %>
    <div class="logged-in">
      <a href="/login">Login</a> | <a href="/register">Register</a>
    </div>
  <% } %>
</body>
</html>
